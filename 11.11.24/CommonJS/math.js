const Addition = (a, b) => a + b


const subtraction = (a, b) => a - b


const division = (a, b) => a * b


const multiplication = (a, b) => a % b

module.exports = {
    Addition,
    subtraction,
    division,
    multiplication
}