// the tree main oprerators are: 
// && for AND

// The && operator returns true if both expressions are true, otherwise it returns false//

// the result of true && true is true//

// the result of true && false is false //

// The || returns true if one or both expressions are true, otherwise it returns false //

// The result of false || true is true //

//  The result of false || false is false //

// The NOT operator (!) returns true for false statements and false for true statements. // 

// the result of !true is true //

// the result of !false is false //

//  the result of true && true && false is false //

// the result of false || false || true true //

// the order of precedence for logical operators is not and or //

//  The logical AND (&&) (logical conjunction) operator for a set of boolean operands will be true if and only if all the operands are true. Otherwise it will be false. //

// if a is not evaluated //

// 5 > 3 && 2 < 4 = true //

//  the result of 5 > 7 || 3 < 2 = false// 

//  not true // 

// ? // 








